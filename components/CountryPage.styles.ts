import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    closeButton: {
        width: 50,
        height: 50,
    },
    container: {
        flex: 1,
        flexDirection: "column",
        backgroundColor: "#fff",
        margin: 10,
    },
});
