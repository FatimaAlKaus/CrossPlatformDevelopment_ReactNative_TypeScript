import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: "column",
    backgroundColor: "#fff",
    margin: 10,
  },
  pressableCountry: {
    borderRadius: 10,
  },
  countryNameText: {
    fontSize: 50,
  },
});
